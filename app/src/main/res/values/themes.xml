<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RetorfitApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/mainNemo</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="colorPrimaryDark">@color/mainNemo</item>
        <item name="colorAccent">@color/mainNemo</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="Theme.RetorfitApp" parent="Base.Theme.RetorfitApp" />
</resources>